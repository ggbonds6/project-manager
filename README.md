# 政府信息化项目管理系统 (project-manager)

面向单位内部的政府信息化项目全生命周期管理系统：**硬件项目 / 软件项目** 的立项 → 招投标/合同 → 实施 → 验收 → 质保运维全过程记录、阶段附件档案与资金付款跟踪。

> 需求/流程/字段/统计口径的权威说明见 [`docs/政府信息化项目管理系统-设计方案.md`](docs/政府信息化项目管理系统-设计方案.md)（设计与实现同步稿，含 §0 修订记录与 §13 实现差异清单，随迭代更新）。**开发迭代进度见 [`ITERATION.md`](ITERATION.md)。**

## 技术栈

| 端 | 选型 |
| --- | --- |
| 前端 `frontend/` | React 18 + TypeScript + Vite + Ant Design 5 + axios + react-router |
| 后端 `backend/` | Java 17 + Spring Boot 3.3 + MyBatis-Plus + 自研迁移 Runner + **崖山 YashanDB（Oracle 模式）** |
| 附件存储 | 抽象 `AttachmentStorage`：默认本地盘，可切 **华为 OBS**（`app.storage.type`，见部署手册） |
| 认证 | JWT（jjwt）+ BCrypt，角色 ADMIN / MANAGER / VIEWER |

## 目录结构

```
.
├─ docs/         设计与部署文档
├─ deploy/       部署资产（docker 生产镜像 / windows 开发脚本）
├─ scripts/      发版打包 + 演示数据 + 开发机辅助工具（清单见 scripts/README.md）
├─ ai-backend/   AI 能力服务（Spring Boot 3 / Java 17，与主系统同栈、独立构建与部署 · ai-1.0.1）
├─ frontend/     React 前端
└─ backend/      Spring Boot 后端
```

配套说明：SQL 执行/演示数据重置等开发机辅助脚本见 [`scripts/README.md`](scripts/README.md)（含每个脚本的用途与用法）。

## 本地运行

前置：JDK 17+、Node 18+、**崖山 YashanDB（Oracle 模式）主备库可达**（默认 10.254.212.106/.107:1688，见 [`deploy/README.md`](deploy/README.md)；启动脚本按平台分类）。

**一键启动（Windows）**：先设置环境变量再双击根目录 `start-dev.cmd`（检查 1688 端口 → 启动后端与前端）；停止用 `stop-dev.cmd`。Docker 方式见 [`deploy/README.md`](deploy/README.md)。

或分步启动：

1. 建库（自研迁移 Runner 在启动时自动执行 `db/migration-yashan/V1~V12` 建表与种子，幂等）：

   ```bash
   # Windows
   set YASHAN_MASTER_IP=10.254.212.106
   set YASHAN_STANDBY_IP=10.254.212.107
   set YASHAN_DB=PM
   set YASHAN_USER=pm
   set YASHAN_PASSWORD=你的密码
   # Linux/macOS: export 同名变量
   ```

2. 后端（连接信息全部来自上方环境变量；密码含特殊字符时整体加引号）：

   ```bash
   cd backend
   mvn spring-boot:run          # http://127.0.0.1:8080/api/health
   ```

3. 前端：

   ```bash
   cd frontend
   npm install
   npm run dev                  # http://localhost:5173（/api 代理到 8080）
   ```

4. （可选）灌入演示数据（**每次迭代功能都应同步补充演示数据**，见 §文档维护约定）：

   ```bash
   # ① 想从干净基线开始时，先物理清空演示数据（可选；详见 scripts/README.md）
   bash scripts/db-sql.sh scripts/demo-reset.sql

   # ② 项目/子项目 + 多合同（施工主合同·监理·第三方测评·预算编制·方案评估）+ 付款 + 项目分工
   node scripts/seed-demo.mjs

   # ③ 三类附件：阶段附件 / 合同附件 / 付款凭证
   node scripts/seed-attachments.mjs
   ```

   > ②③ 走真实后端 API，默认连 `http://127.0.0.1:8088`（可传参改，如 `node scripts/seed-demo.mjs http://127.0.0.1:8080`）。
   > 注意：本系统删除一律是**逻辑删除**，反复重跑 seed 会在库里堆积 `deleted=1` 的历史行——看数据库时先按 `deleted=0` 过滤，或先用 ① 清空。
   > 需要手工查/改库（本机通常没有 yasql 客户端）时用 `bash scripts/db-sql.sh <sql文件>`。

预置登录账号（密码均 `123456`）：`admin`（管理员）/ `jingban01`（经办人）/ `lingdao01`（领导，只读）。

> 本地开发用 Windows 一键脚本；生产用 Docker 一体化镜像（前后端打包，数据库为外部崖山）：
> 见 [`deploy/README.md`](deploy/README.md)（含环境变量、建库说明）。
> Git 协作的 SSH 配置见 [`docs/GITHUB-SSH-setup.md`](docs/GITHUB-SSH-setup.md)。

## 接口约定

统一响应 `{ code, message, data }`，`code=0` 成功；写操作以 `POST/PUT/DELETE` + `/api/...` 前缀；附件经 `/uploads/**` 静态访问。

## 数据库

- **崖山 YashanDB（Oracle 模式）主备集群**，驱动级 `primary` 多地址 + TAF 高可用（详见 `docs/崖山oracle模式迁移实施方案.md`）。
- 迁移脚本：`backend/src/main/resources/db/migration-yashan/`（V1~V12），启动时由 `YashanMigrationRunner` 自动执行。
- JDBC 驱动 `yashandb-jdbc-1.9.3.jar` 位于 `backend/lib/`（system scope 本地引入；接入内网 Nexus 后可改普通依赖）。
- 主键统一 **identity**（`GENERATED BY DEFAULT AS IDENTITY`），实体保持 `@TableId(type = AUTO)` 不动。

## 文档维护约定（硬性）

> 本仓库以"代码与文档同步"为**硬性指标**：每完成一个可验证的迭代，**必须**同步更新并一并提交——

1. `ITERATION.md`：**「迭代总表」新增一行** *且* **「各迭代明细」补一个对应版本段**（两处版本集合必须一一对应）；
   同时更新「功能完成度」「运行方式速查」「后续待办（Backlog）」，并**删除**任何"待提交/半成品"占位说明；
2. `docs/政府信息化项目管理系统-设计方案.md`：§0 修订记录（版本表）、受影响章节（§5 字段含 §5.3/§5.4/§5.5、§6.2 页签、§8 数据模型表数量、§9 统计口径等）、§13 差异清单；
3. 本文档 `README.md`：技术栈 / 功能 / 运行方式 / 部署等与当前实现保持一致；
4. 涉及脚本时同步更新 `scripts/README.md`（脚本清单与用法）；
5. **每次功能迭代都要用新增/修改演示数据把该功能体现出来**（含边界场景：缺件、异常、风险项），
   让系统可直接演示——这是固定交付项，不是可选项；
6. 收尾顺手清理无效代码（未使用导入 / 死方法 / 只写不读的变量）；
7. 提交说明引用对应验证结果。

当前版本：**v3.5**（数据库已切换崖山 YashanDB Oracle 模式）（详见 [`ITERATION.md`](ITERATION.md) 迭代总表与功能完成度）。
